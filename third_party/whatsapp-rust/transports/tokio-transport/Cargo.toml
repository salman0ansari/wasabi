[package]
name = "whatsapp-rust-tokio-transport"
version = "0.7.0"
edition = "2024"
rust-version.workspace = true
authors = ["João Lucas <jlucaso@hotmail.com>"]
license = "MIT"
repository = "https://github.com/jlucaso1/whatsapp-rust"
description = "Tokio-based WebSocket transport for whatsapp-rust"

[features]
default = []
# Skip TLS certificate verification (for testing with mock servers)
danger-skip-tls-verify = []

[dependencies]
anyhow = { workspace = true }
async-channel = { workspace = true }
async-trait = { workspace = true }
bytes = { workspace = true }
futures-util = { version = "0.3.33", default-features = false, features = ["sink", "alloc", "std"] }
http = "1.5.0"
log = { workspace = true }
rustls = { version = "0.23", default-features = false, features = ["ring"] }
tokio = { workspace = true, features = ["macros", "rt", "sync"] }
tokio-rustls = { version = "0.26.4", default-features = false, features = ["ring"] }
tokio-websockets = { version = "0.13.3", features = [
    "client",
    "rustls-bring-your-own-connector",
    "rand",
    "ring",
] }
wacore = { workspace = true }
webpki-roots = "1.0.9"

[dev-dependencies]
# The upgrade-header tests read the request off a local socket. Dev-only, so the
# release dependency set — and the binary-size gate — are unaffected.
tokio = { workspace = true, features = ["io-util", "macros", "net", "rt", "time"] }

[lints]
workspace = true
